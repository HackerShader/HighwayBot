const consolelog = require('./util/translate');

module.exports = {
    name: "language",
    description: "Language option",
    aliases: ['lang'],
    /**
     *
     * @param {String[]} args
     */
    async execute(args) {
        if (!args) {
            
        }
    }
};