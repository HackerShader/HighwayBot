import chalk from 'chalk'
import fs from 'node:fs'
import { CommandBuilder } from './module'
console.log(
    `.----------------------------------------.` +
    `|                                        |` +
    `|          HighwayBot Controller         |` +
    `|                                        |` +
    `'----------------------------------------'`
)